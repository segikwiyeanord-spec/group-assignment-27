# group-assignment-27
The project is implemented using Oracle SQL and PL/SQL. It includes a relational database with tables for members and attendance records. The system demonstrates important database programming concepts such as anonymous blocks, stored procedures, functions, and window functions to manage data and produce useful attendance reports.
